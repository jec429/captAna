#ifndef ECaptainSummary_hxx_seen
#define ECaptainSummary_hxx_seen

#include <ECore.hxx>

namespace CP {
    /// Generate a base exception for the Analysis library
    EXCEPTION(ECaptainSummary,ECore);
    EXCEPTION(EDataFile, ECaptainSummary);
};
#endif
